/** A resolved scheme after system preference has been applied. */
export type ThemeMode = 'light' | 'dark';

/** The preference accepted by the presentation theme provider. */
export type ThemePreference = ThemeMode | 'system';
